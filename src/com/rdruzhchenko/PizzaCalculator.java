package com.rdruzhchenko;

public class PizzaCalculator {

    public OutputData calculate(InputData inputData) {

        OutputData outputData = new OutputData();

        // Iterate
        // If sum == max
        //  return indexes

        outputData.pizzasIndexes = new Integer[] { 0, 2, 3 };
        return outputData;
    }
}
